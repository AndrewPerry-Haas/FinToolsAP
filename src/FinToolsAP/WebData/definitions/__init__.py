"""
FinToolsAP.WebData.definitions
===============================

Built-in characteristic definitions.

Every ``.py`` file in this package (except those starting with ``_``) is
scanned by :func:`~FinToolsAP.WebData.registry.load_builtins`.  Any
top-level callable with a ``.needs`` attribute will be auto-registered.
"""
